node_modules/
dist/
.env
*.js.map
*.d.ts.map

# Prisma
backend/prisma/migrations/**/migration_lock.toml

# Frontend build
frontend/dist/

# Local git worktrees (termag creates these under <project>/.worktrees/<name>
# for non-main workstreams; they're embedded checkouts, never part of this repo)
.worktrees/

# Relay config (contains tokens)
relay/relay.config.json

# Agent config (contains tokens)
agent/agent.config.json
agent/agent.config.*.json

# Claude Code's local state for this repo
.claude/

# Packer (generated by the build)
packer/packer-manifest.json

# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfstate.backup
*.tfvars
!*.tfvars.example
*.tfplan
tfplan

# OS
.DS_Store
packer/termag.bundle
packer/termag.bundle.origin
packer/nitrotpm-ami-id
